<div class = "smurfy span4 offset4 text-center">
	<h2>Enter your Allegiance</h2>
	<%= simple_form_for @pic do |f| %>
		<%= f.input :title, label: 'Enter the House', placeholder: 'Which of the 7 Kingdoms' %>
		<%= f.input :subtitle %>
		<%= f.input :message %>

		<%= f.submit "Save", :class => 'btn btn-primary' %>
	<% end %>

</div>
